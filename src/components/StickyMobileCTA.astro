---
interface Props {
  title?: string;
  buttonText?: string;
  affiliateUrl?: string;
  rating?: number;
  // ... other props
}

const { 
  title = "Ready to upgrade?", 
  buttonText = "Check Price", 
  affiliateUrl = "#", 
  rating = 4.8 
} = Astro.props;
---